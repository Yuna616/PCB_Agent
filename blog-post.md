# EasyEDA PCB 에이전트 크롬 확장 개발기

> AI가 PCB 설계 과정을 옆에서 코치해준다면 어떨까? 라는 질문에서 시작한 사이드 프로젝트.

---

## Project Vision

EasyEDA로 PCB를 처음 설계하는 사람이 가장 많이 하는 실수가 있다. 데커플링 캐패시터를 빠뜨리거나, GND 핀을 플로팅으로 남기거나, 트레이스 폭을 전류량 무시하고 그냥 0.2mm로 쭉 그어버리는 것들이다. 이런 실수는 DRC로도 잡히지 않는 경우가 많고, 보드가 실제로 제작되어 돌아오고 나서야 "아, 이게 문제였구나" 하고 깨닫게 된다.

문제는 **이 지식이 검색하면 다 나오는 내용인데도, 설계하는 순간에는 생각이 안 난다**는 점이다. 스케매틱 그리는 데 집중하다 보면 보호 회로 생각이 안 나고, 라우팅에 집중하다 보면 임피던스 매칭 생각이 안 난다.

그래서 만들고 싶었던 건 간단하다. **EasyEDA를 열면 옆에 따라붙어서, 지금 이 순간 필요한 조언을 바로 해주는 AI 코치.** 프로젝트 문서를 올리면 회로 구성을 파악하고, 어떤 IC가 필요한지, 데커플링은 어떻게 해야 하는지, 보호 회로는 뭘 넣어야 하는지 단계별로 알려준다. GPT-4o가 뒤에서 돌아가고, 크롬 확장이 EasyEDA 위에 패널로 올라가는 구조다.

---

## Project Scope

확장 프로그램이 해야 할 일의 범위를 처음에 명확히 잡아두지 않으면 금방 산으로 간다. 그래서 "할 것"과 "안 할 것"을 나눠서 정리했다.

**할 것**

- EasyEDA(pro.easyeda.com, oshwlab.com) 위에 플로팅 패널 주입
- 프로젝트 문서(`.txt`, `.md`, `.json`, `.ino`, `.sch` 등 텍스트 기반) 파일 업로드 및 파싱
- 문서를 GPT-4o로 분석 → 전원 요구사항, 통신 인터페이스, 추천 부품 등 구조화된 JSON으로 추출
- 추출된 컨텍스트를 대화에 주입해서 프로젝트 맞춤 조언 제공
- PCB 설계 단계별 체크리스트 제공 (스케매틱 / PCB 레이아웃)
- 자주 쓰는 질문 빠른 버튼 (보호 회로 검토, 트레이스 폭 계산, 거버 파일 생성 등)
- 가상 회로 미러(Virtual Circuit Mirror)로 부품 배치/연결 상태 추적

**안 할 것**

- EasyEDA API를 직접 제어해서 부품을 자동으로 배치하는 것 — EasyEDA는 안정적인 공개 JS API가 없다. 시도는 해봤는데 minified 변수명이 릴리즈마다 바뀐다. 불안정한 hook에 의존하는 건 유지보수 재앙이 될 것 같아서 포기.
- 이미지/PDF 파일 파싱 — 데이터시트를 올려서 분석하면 좋겠지만 이건 범위 밖. 텍스트 기반 파일만 처리한다.
- 클라우드 싱크, 계정 시스템 — 키는 로컬 스토리지에 저장하는 걸로 충분하다.

---

## 구현 계획

### 아키텍처

MV3(Manifest Version 3) 기반 크롬 확장이다. 크게 세 레이어로 나뉜다.

```
[ EasyEDA 탭 ]
    contentScript.js   ← UI 패널 주입, 사용자 입력 처리
    messageBridge.js   ← content ↔ background 통신 추상화
    circuitEngine.js   ← 가상 회로 미러, 부품 라이브러리, DRC 힌트

[ Service Worker ]
    background.js      ← OpenAI API 호출, 시스템 프롬프트 관리

[ Popup ]
    popup.html/js      ← API 키 설정
```

Content script가 OpenAI API를 직접 호출할 수 없는 건 MV3의 CORS 정책 때문이다. 모든 API 호출은 background service worker를 통해서 한다. `messageBridge.js`가 `chrome.runtime.sendMessage` 위에 Promise 래퍼를 씌워서 content script 쪽 코드가 깔끔하게 `await BRIDGE.send(...)` 형태로 쓸 수 있게 했다.

### AI 프롬프트 설계

가장 공을 많이 들인 부분이 여기다. 시스템 프롬프트에 PCB 설계 지식을 얼마나 잘 녹여넣냐가 응답 품질의 90%를 결정한다.

두 개의 시스템 프롬프트를 운용한다.

**`SYSTEM_ANALYZE_DOCS`** — 파일 업로드 시 1회 실행. 문서에서 전원 요구사항, 통신 인터페이스, 추천 부품, PCB 설계 고려사항을 추출해서 JSON으로 반환한다. `response_format: { type: "json_object" }`를 써서 파싱 실패를 방지한다.

**`SYSTEM_CHAT`** — 대화마다 사용. 분석된 JSON을 시스템 컨텍스트로 주입하고, PCB 설계 전문 지식을 상세하게 내장했다. 프롬프트에 포함된 내용들:

- 스케매틱 설계 원칙 (블록 분리, 넷 이름 규칙, 바이패스 캐패시터 필수 규칙)
- PCB 레이아웃 원칙 (배치 순서, 트레이스 폭 계산식, 접지 플레인 전략, EMI/EMC)
- EasyEDA 실무 작업 순서 (스케매틱 8단계, 레이아웃 9단계)
- 부품 선택 기준 (레귤레이터, 통신 IC, MCU 별 상황별 추천)
- 설계 검증 체크리스트

"구체적인 값을 항상 함께 제시한다"는 지시를 명시적으로 넣었다. 이게 없으면 AI가 "저항이 필요합니다" 같은 쓸모없는 답변을 한다. 값이 있어야 "10kΩ 0402 풀업 저항이 필요합니다"가 된다.

### 부품 라이브러리 (`circuitEngine.js`)

처음에는 부품 타입 14개로 시작했는데, 실제로 쓰다 보니 자꾸 모르는 부품 타입이 나온다. 결국 50종 이상으로 확장했다. 각 부품 항목에 들어있는 필드:

| 필드 | 역할 |
|---|---|
| `query` | EasyEDA 라이브러리 검색 키워드 |
| `package` | 패키지 타입 (SOT-223, 0402 등) |
| `lcsc` | LCSC 부품번호 — 이걸 있으면 바로 발주 가능 |
| `decoupling` | 필요한 바이패스 캐패시터 스펙 |
| `layout_tip` | PCB 배치 시 주의사항 |

여기에 설계 규칙 검사(`DESIGN_RULES`) 함수도 붙였다. 현재 가상 회로 상태를 받아서 데커플링 캐패시터 누락, CAN 종단 저항 누락, USB ESD 보호 누락, 역극성 보호 누락 등을 자동으로 진단한다.

### UI — 탭 기반 패널

초기에는 모든 걸 세로로 쌓아놓은 단일 패널이었다. 체크리스트, 파일 업로드, 빠른질문, 채팅이 전부 한 화면에 있었는데, 실제로 써보니 스크롤이 너무 많이 필요하고 화면이 답답했다.

탭으로 분리했다. 4개 탭:

- **💬 AI 코치** — 메인 채팅. 마크다운 간이 렌더링 포함 (코드블록, 굵게, 목록)
- **📂 문서** — 파일 업로드/관리
- **✅ 체크리스트** — 스케매틱/PCB 레이아웃 단계별 체크박스
- **⚡ 빠른질문** — 자주 쓰는 질문 12개 버튼. 클릭하면 자동으로 AI 코치 탭으로 이동해서 질문 전송

파일 업로드 후 분석이 완료되면 자동으로 AI 코치 탭으로 전환된다. 사용자가 탭을 찾아 클릭할 필요 없이 흐름이 자연스럽게 이어지도록.

### 삽질 기록

**EasyEDA API 시도**: `window.EasyEDA`, `window.editor`, `window.lc` 등 가능한 글로벌 변수를 다 탐색해서 `addComponent`, `connect` 같은 함수를 호출하려 했다. 실제로 동작하는 환경이 있긴 한데, 릴리즈마다 다르고 Pro 버전과 Standard 버전이 또 다르다. 결국 "찾으면 쓰고, 없으면 가상 미러에만 기록"하는 방어적 구조로 바꿨다.

**MV3 storage 문제**: `chrome.storage.sync`가 특정 환경에서 조용히 실패한다. 할당량 초과, 정책 제한 등 이유가 여러 가지다. `local`을 우선 시도하고 실패 시 `sync`로 폴백하는 구조로 바꿨다.

**API 키 보안**: 키를 content script에서 직접 들고 있으면 페이지 JS에 노출될 위험이 있다. background service worker가 키를 들고 있고, content script는 "이 내용으로 API 호출해줘"만 요청하는 구조다. 키 자체는 content script가 절대 볼 수 없다.

---

다음 단계로 생각하고 있는 건 스트리밍 응답 지원이다. 지금은 응답이 완성될 때까지 기다렸다가 한꺼번에 표시하는데, 긴 답변은 체감 대기 시간이 꽤 길다. `stream: true`로 바꾸고 SSE를 파싱해서 타이핑 효과로 보여주면 훨씬 나을 것 같다.
